/**
 * @enabled
 *
 * @prefix-name multi-example
 * @prefix-enabled
 */

// You can use this command with both "!multi-example" and "/multi-example", how cool is that!

await interaction.reply({content: "This is an example command, " + args.test + "!", ephemeral: true});